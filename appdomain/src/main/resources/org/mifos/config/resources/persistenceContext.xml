<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:sec="http://www.springframework.org/schema/security"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
      http://www.springframework.org/schema/tx
      http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
      http://www.springframework.org/schema/context
      http://www.springframework.org/schema/context/spring-context-3.1.xsd
      http://www.springframework.org/schema/aop
      http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
      http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
      http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <context:annotation-config/>
    <aop:aspectj-autoproxy/>

    <tx:annotation-driven transaction-manager="platformTransactionManager" order="200"/>

    <import resource="queryInterceptorContext.xml" />

    <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean" destroy-method="destroy">
        <property name="configLocations">
          <list>
              <value>classpath:org/mifos/platform/rest/approval/domain/rest_hibernate.cfg.xml</value>
              <value>classpath:org/mifos/platform/questionnaire/domain/questionnaire_hibernate.cfg.xml</value>
              <value>classpath:org/mifos/platform/cashflow/domain/cashflow_hibernate.cfg.xml</value>
              <value>classpath:org/mifos/framework/util/resources/hibernate.cfg.xml</value>
          </list>
        </property>
        <property name="hibernateProperties">
            <ref bean="dbSpecificHibernateProperties"/>
        </property>
        <property name="namingStrategy">
            <ref bean="namingStrategy"/>
        </property>
    </bean>


    <!--<bean id="hibernateUtil" class="org.mifos.framework.hibernate.helper.HibernateUtil">-->
        <!--<property name="sessionFactory" ref="sessionFactory" />-->
    <!--</bean>-->

    <bean id="hibenateUtilTx" class="org.mifos.framework.hibernate.helper.HibernateUtilAspect"/>

    <bean id="namingStrategy" class="org.hibernate.cfg.ImprovedNamingStrategy"/>

    <bean id="platformTransactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <tx:advice id="txAdvice" transaction-manager="platformTransactionManager">
        <tx:attributes>
            <!-- all methods starting with 'get', 'find', 'list', 'iterate', 'retrieve' are read-only -->
            <tx:method name="get*" read-only="true"/>
            <tx:method name="find*" read-only="true"/>
            <tx:method name="list*" read-only="true"/>
            <tx:method name="retrieve*" read-only="true"/>
            <tx:method name="*" rollback-for="java.lang.Exception"/>
            <!-- other methods use the default transaction settings (see below) -->
            <tx:method name="*"/>
        </tx:attributes>
    </tx:advice>
    <!-- ensure that the above transactional advice runs for any execution
         of an operation defined by the ServiceService interface -->
    <aop:config>
        <aop:pointcut id="mifosServiceOperation" expression="execution(* org.mifos.platform..*ServiceFacade.*(..))"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="mifosServiceOperation"/>
    </aop:config>
</beans>
