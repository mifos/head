SeedDataManyClients.py
    * python script for Selenium RC.  Requires Python 2.5 and Selenium-RC to run.
    * Generates multiple offices, centers, groups, and clients.
	# change these numbers to set up different combinations
`		NUM_OFFICES = 1
		NUM_CENTERS_PER_OFFICE = 1
		NUM_GROUPS_PER_CENTER = 1
		NUM_CLIENTS_PER_GROUP = 1
    * Populates an empty dev instance with one or more groups, centers, clients, loans, etc.
      Works well against local Mifos instance (baseURL set to something like
      http://localhost:8080). Should only be used against your own instance of Mifos.
